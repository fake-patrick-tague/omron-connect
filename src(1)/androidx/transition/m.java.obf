package androidx.transition;

import android.view.ViewGroup;

public class h
{
  private ViewGroup a;
  private Runnable b;
  
  public static h b(ViewGroup paramViewGroup)
  {
    return (h)paramViewGroup.getTag(f.c);
  }
  
  static void c(ViewGroup paramViewGroup, h paramH)
  {
    paramViewGroup.setTag(f.c, paramH);
  }
  
  public void a()
  {
    if (b(a) == this)
    {
      Runnable localRunnable = b;
      if (localRunnable != null) {
        localRunnable.run();
      }
    }
  }
}
