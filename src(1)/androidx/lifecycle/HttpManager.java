package androidx.lifecycle;

import android.view.View;
import androidx.lifecycle.xy.R.style;

public class HttpManager
{
  public static void init(View paramView, h paramH)
  {
    paramView.setTag(R.style.tag, paramH);
  }
}
