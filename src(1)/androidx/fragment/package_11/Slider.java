package androidx.fragment.package_11;

import v7.util.ArrayMap;

class Slider
  implements Runnable
{
  Slider(k paramK, SpecialEffectsController.Operation paramOperation1, SpecialEffectsController.Operation paramOperation2, boolean paramBoolean, ArrayMap paramArrayMap) {}
  
  public void run()
  {
    Frame.a(i.next(), w.next(), h, x, false);
  }
}
